<div class="col-md-4 col-md-offset-4">
  <div class="page-header text-center">
    <h3><%= t '.title' %></h3>
  </div>

  <%= simple_form_for @main_module do |form| %>
    <%= render 'form', document: @document, form: form %>
  <% end %>
</div>
