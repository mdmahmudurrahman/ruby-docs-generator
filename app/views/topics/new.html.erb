<div class="col-md-4 col-md-offset-4">
  <div class="page-header text-center">
    <h3><%= t '.title' %></h3>
  </div>

  <%= simple_form_for [@sub_module, @topic] do |form| %>
    <%= render 'form', sub_module: @sub_module, form: form %>
  <% end %>
</div>
